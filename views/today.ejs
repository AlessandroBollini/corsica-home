<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>La frase del giorno</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Include your CSS -->
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #f4f4f4;
            font-family: Arial, sans-serif;
            text-align: center;
        }
        .quote-container {
            max-width: 80%;
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .quote-text {
            font-size: 24px;
            margin-bottom: 20px;
        }
        .update-form {
            margin-top: 20px;
        }
        .update-form input[type="text"] {
            width: 80%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 16px;
        }
        .update-form button {
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            background-color: #007bff;
            color: white;
            font-size: 16px;
            cursor: pointer;
            margin-left: 10px;
        }
        .update-form button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="quote-container">
        <div class="quote-text">
            <%= today %>
        </div>
        <br></br>
            <div class="bottombanner">
                <button onclick="window.location.href='/'">Home</button>
            </div>
        <form action="/today" method="POST" class="update-form">
            <input type="content" name="content" placeholder="" required>
            <button type="submit">Cambia la frase</button>
        </form>
    </div>
</body>
</html>
